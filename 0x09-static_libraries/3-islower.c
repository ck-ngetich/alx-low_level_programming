#include "main.h"
/**
 * _islower - it checks for the lowercase
 * @c: is the characters to check
 * Return: 1 if c is lowercase else 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
